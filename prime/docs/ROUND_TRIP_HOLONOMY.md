# Round-trip holonomy (TOPICALITY)

```
C ──write RESTRICT──▶ R ──reconstruct──▶ C′
└──────────── drift = 1 − nomic_cos ────┘
```

**Purpose:** `TOPICALITY` (earned nomic, stripped+prefix).  
**Not** AGREEMENT. **Not** production OPEN.

## Why not a mirror

- Routes through the instrument that passed its aboutness null (range ~0.45).
- Null: real vs paraphrase vs pasta — if drifts equal → noise; if structure → holonomy.
- Same shape as peptide ring / Mag-PI tour: go around, come back, residue or close.

## CLI

```bash
python prime/scripts/round_trip_holonomy.py null          # three-way null first
python prime/scripts/round_trip_holonomy.py hop --claim "..."
python prime/scripts/round_trip_holonomy.py sweep --claim "..."
python prime/scripts/round_trip_holonomy.py second --claim "..."
```

## Arrival / certificate

If C′ ≈ C, R held the whole proposition (logogram-shaped compression).  
If C′ drifts, R was claim-shaped noise. Certificate is still the production logogram; this tests *semantic* closure of RESTRICT.

## Ordering

1. null  
2. single hop  
3. compression sweep (S-knee / MDL proxy)  
4. frontier arm Δh (optional)  
5. ent_low human eyes still open for NLI eval  
