# Two objects named OPEN (do not confuse)

| Name | Where | Means | Evidence bar |
|------|--------|--------|--------------|
| **ACCEPTED** (`WORK_ACCEPTED`) | deep_loop items | Work item accepted for *tracking* | LFM roles + NLI/aboutness pack gates |
| **OPEN** (`AUDIT_OPEN`) | session audit / claims | Survived **measures under law** | RESTRICT + purpose-matched measures + audit |

**OP02-class claims** (orders-of-magnitude compute reduction) can be **ACCEPTED** for tracking while remaining **not production OPEN**. Citing deep-loop OPEN as verified was the failure mode; that field is now **ACCEPTED**.

Legacy deep jobs: `OPEN` → `ACCEPTED` on load.

## Purpose gate

```
RETRIEVE / TOPICALITY  → nomic
AGREEMENT              → NLI only
IDENTITY               → symbol jaccard (code; no embedder)
EXISTENCE              → smoke / rplc / tests / filesystem
```

Production audit OPEN requires **AGREEMENT + EXISTENCE** purposes present.  
AGREEMENT claim + cosine only → **type error STOP**.

See `purpose_gate.py`.

## Five-nulls spine (same tell)

What can be wrong upstairs that couldn't downstairs? No answer → rename, not lift.

| # | Lift | Fate |
|---|------|------|
| cosine glue_ok = agreement | pasta≈peptide | retired |
| LFM-NLI 3/3 smoke | con_high/ent_low null | **not earned** (56.9% overall) |
| deep OPEN as production | renamed ACCEPTED | fixed |

## Code route

No embedder — `interface_jaccard` / exact symbol only.
