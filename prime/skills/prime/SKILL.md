---
name: prime
description: >
  One-door OPEN|STOP development: /prime boots session MCP + compute graph + operator law.
  Use for lineage, rplc, multiplane, sheaf gates, LM Studio scouts, long topological sessions.
  Prefer this over fragmented rplc/idea/harness/sheaf slash commands.
---

# Prime skill

## Law

```
restrict → measure → audit → OPEN | STOP
residue never forced
```

## User surface

| Command | Meaning |
|---------|---------|
| `/prime [intent]` | Start/continue |
| `/prime status` | Status only |
| `/prime halt` | Cert + halt |

## MCP (prime-session)

**Every user enter after session start:** call **`exchange`** with their text (LFM + projection + measures).

Also: `session_*`, `restrict`, `graph_*`, `measure`, `condition_*`, `audit`, `cert_write`, `ask_*`, `lm_*`, `meta_loop`, `enter_projection`.

Skipping `exchange` drops local LLM modality — do not skip.

## Measure modes

| mode | Instrument |
|------|------------|
| smoke | tests/test_smoke.py |
| rplc | rplc_sheaf CLI |
| eref | topology E_ref prior |
| lm / lm_scout | LM Studio :1234 |
| **project** | **bilateral language projection (human ↔ domains)** |
| all | smoke+rplc+models+projection |

## Language = bilateral projection (operational)

Neither human chat nor domain jargon is the manifold. Both are **projections**.

| Side | MCP | Stalk source |
|------|-----|----------------|
| Human | `project_human` | intent + restrict |
| Domain | `project_domain` | code \| rplc \| eref \| field \| lm |
| Glue | `project_align` | interface Jaccard + feature mix |

**Graph nodes:** PROJECT → ALIGN (before CODE when intent is language-heavy).

**Audit rule:** if last alignment is `all_frustrated` → OPEN blocked.

Regimes: `glue_ok` | `partial_section` | `frustrated` (helper tag, not pure λ₁).

## Resources (CPU/GPU/NPU/RAM)

This field kit is Snapdragon-class: **CPU parallel measures**, **LM Studio for GPU/NPU**, **RAM headroom**.

| Tool | Use |
|------|-----|
| `resource_status` | Snapshot + plan |
| `measure_parallel` | Fan-out smoke+rplc+projection |
| `measure(resource)` | Same as resource_status |

Prefer LFM/Granite scouts; Bonsai only if free RAM high or already loaded. See `docs/RESOURCES.md`.

## LM Studio = multi-fiber projection (not chat)

See `docs/LM_STUDIO_PROJECTION.md`.

| Tool | Meaning |
|------|---------|
| `enter_projection` | **Every enter** → multi-model fibers + embed cosine + votes |
| `lm_load` / `lm_unload` | Resident fiber control (native v1 API) |
| `lm_embed` | 768-d metric on language |
| `lm_models` | Native catalog + loaded_instances + roster |
| `lm_scout` | Single fiber |

Ensemble disagreement = residual stream, not OPEN.
## Accuracy

`ask_need` when guessing would corrupt the audit.

## Not claimed

Any-idea→reality; backbone P2 without instrument; force-OPEN; LM as proof; language alone as global section.
