---
name: multiplane-harness
description: >
  Use for multi-plane agentic harness work: local LM Studio scouts (LFM/Bonsai),
  dump+EMZ ingest, claim bundles, external OPEN|STOP certify, Geometry of Being
  operational face. Triggers on harness-pipeline, certify claims, multi-plane,
  explore vs certify, drag-drop dumps, LM Studio scout.
---

# multiplane-harness skill

## Design law

```
restrict → measure relations → draft → CERTIFY externally → OPEN | STOP
```

Residue never forced. LLMs never self-OPEN multi-plane truth.

## Layout (`${GROK_PLUGIN_ROOT}` / PRIMEdEV-1/harness)

| Path | Role |
|------|------|
| `pipeline/v1/pipeline.py` | scout → bundle → certify |
| `certify/v1/certify.py` | external gate |
| `ingest/v1/ingest.py` | dump + emz → planes |
| `local_mode/lfm_scout_v1` | fast scout |
| `local_mode/bonsai_scout_v1` | deep scout |
| `commands/` | slash recipes |

## Agent rules

1. Prefer `/harness-pipeline` or `pipeline.py` over freeform "verified" language.
2. Local scouts: system explore-only; parse drafts as DRAFT only.
3. Pair with rplc-sheaf / sheaf-guardian when Laplacian gating needed on numeric X.
4. Do not claim medical/domain discovery from structural OPEN.
